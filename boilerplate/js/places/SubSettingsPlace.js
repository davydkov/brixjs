define(['places/SettingsPlace'], function (SettingsPlace) {
    /**
     * @extends {SettingsPlace}
     */
    return SettingsPlace.extend({
        name: 'sub-settings' // Just for debugging
    });
});